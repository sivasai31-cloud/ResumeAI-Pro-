# Development API URL.
# When omitted, defaults to '/api' which is securely proxied by Vite to http://127.0.0.1:8000,
# completely avoiding browser mixed-content (HTTPS frontend -> HTTP backend) issues.
# VITE_API_URL=/api
